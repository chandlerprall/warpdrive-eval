import Store from '@ember-data/store';

// Minimal store service to bootstrap WarpDrive/Ember Data for subsequent iterations.
// We will layer request manager configuration and schemas as we add features.
export default class ApplicationStoreService extends Store {}

